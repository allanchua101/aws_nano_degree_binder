from setuptools import setup

setup(name='pogs_dsnd',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['pogs_dsnd'],
      author="Allan Chua",
      zip_safe=False)
